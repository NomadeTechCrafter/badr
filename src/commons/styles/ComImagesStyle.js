import {loginHeaderImage as loginHeaderImageSize} from './ComSizeStyle';
import {loginHeaderImage as loginHeaderImageSpacing} from './ComSpacingStyle';

import {menuUserImage as menuUserImageSize} from './ComSizeStyle';
import {menuUserImage as menuUserImageSpacing} from './ComSpacingStyle';

export const loginHeaderImage = {
  ...loginHeaderImageSize,
  ...loginHeaderImageSpacing,
};

export const menuUserImage = {
  ...menuUserImageSize,
  ...menuUserImageSpacing,
};
